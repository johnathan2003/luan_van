"""
ĐÃ CHUYỂN — file này không còn được dùng.

Nội dung đã di chuyển sang `../../../chatbot/bot_service.py` (service
chatbot riêng, ngang hàng với `backend/`, chạy port 8002). Xem
`chatbot/main.py` để biết lý do tách (chuẩn bị cho thiết bị IoT ESP32-S3
gọi thẳng vào chatbot sau này).

File này được giữ lại (rỗng) chỉ vì môi trường sandbox lúc thực hiện việc
tách không xoá được file vật lý — bạn có thể tự xoá file này trên máy
mình, không có gì trong backend còn import module này nữa.
"""
